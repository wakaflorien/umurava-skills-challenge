export type ButtonProps = {
  classNames: string;
  label: string;
  onClick: () => void;
};

export type DivProps = {
  icon: string;
  title: string;
  desc: string;
  iconWidth: number;
  iconHeight: number;
};
